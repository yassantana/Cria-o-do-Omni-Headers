/**
 * Barrel da camada de dados (conteudo estatico e dados de exemplo dos mockups).
 */
export { siteConfig } from './site'
export type { SiteConfig } from './site'
export {
  mockConversations,
  mockThread,
  statusLabel,
  statusColor,
  channelVolume,
} from './mock'
