# Exemplo de variaveis de ambiente do Omni Headers.
# Copie para `.env` e ajuste os valores. Apenas variaveis com prefixo VITE_
# sao expostas ao client (ver src/types/env.d.ts).

# URL base da API (quando houver backend)
VITE_API_URL=

# Identificacao do site
VITE_SITE_NAME=Omni Headers
